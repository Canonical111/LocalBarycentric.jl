var documenterSearchIndex = {"docs":
[{"category":"section","location":"theory/#Theory","page":"Theory","text":"","title":"Theory"},{"category":"section","location":"theory/#The-second-barycentric-form","page":"Theory","text":"Given s distinct nodes x_1  dots  x_s and values f_i, the unique degree-(s-1) interpolating polynomial can be evaluated as\n\np(x) = fracdisplaystylesum_i=1^s fracw_ix - x_i f_i\n                displaystylesum_i=1^s fracw_ix - x_i\nqquad\nw_i = prod_j neq i frac1x_i - x_j\n\nThis second (true) barycentric form is backward-stable and — unlike the Newton or Vandermonde routes — evaluates in O(s) once the weights are known. It is well behaved arbitrarily close to nodes (tested here at 10^-15 relative separation), and an exact node hit is returned directly. The weights are computed once per window in O(s^2) and cached (barycentric_weights is pure, so cached and recomputed weights are bit-identical).","title":"The second barycentric form"},{"category":"section","location":"theory/#Local-windows,-not-global-polynomials","page":"Theory","text":"A global polynomial through many equispaced nodes is destroyed by the Runge phenomenon. This package instead slides a local window of order + 1 nodes across the grid — the same convention as Mathematica's Interpolation[..., InterpolationOrder -> order] — and evaluates the window's polynomial. For a query near the center of its window, the equispaced Lebesgue constant (the factor by which input errors are amplified) stays O(1): measured 1.9 at degree 28. High order is therefore free in the interior: interior accuracy sits at the data rounding floor at every tested degree up to 120.\n\nOne weight row is precomputed per window position, so hot-loop evaluation never recomputes weights (except in the tapered edge zone, where windows are narrower than the cached full width and the O(s^2) recompute is cheaper than the interior mat-vec anyway).","title":"Local windows, not global polynomials"},{"category":"section","location":"theory/#The-edge-problem-and-the-graded-taper","page":"Theory","text":"Near a domain boundary the window cannot center; a full-width window clamps and the query sits at the window's edge, where the equispaced Lebesgue function grows like 2^s. Measured at degree 28, grid step 1101, Float64 data:\n\nquery position Lebesgue relative error with exact data\nsubinterval 1 (at the boundary) 11 times 10^6 39 times 10^-13 3 times 10^-27\nsubinterval 5 216 20 times 10^-15 9 times 10^-31\ninterior (centered) 19 20 times 10^-16 4 times 10^-32\n\nThe last column shows the scheme itself is nearly exact even at the edge — the error is amplified input rounding, so no amount of higher-precision arithmetic recovers it. The cure is geometric: stencil_window tapers the width to\n\ns(k) = operatornameclampbigl(2k s_min textorder+1bigr)\n\nwhere k counts nodes on the thinner side of the query, so the query stays near the window center. The floor s_min = textttedge_order_min + 1 (default degree 12, i.e. 13 nodes) balances the two error terms: amplification sim 2^s falls with s while truncation sim (ch)^s rises, and at degree 12 on fine grids both sit below the Float64 data floor. Measured effect at degree 28: worst boundary error improves from 5 times 10^-11 to 4 times 10^-15 (~14 000×), with interior queries bit-identical to the untapered scheme. More points near a boundary make edge evaluation worse, not better — the taper deliberately uses fewer.","title":"The edge problem and the graded taper"},{"category":"section","location":"theory/#Precision-semantics","page":"Theory","text":"Three types cooperate:\n\nTN — node storage type;\nTW = weight_type — the type weights are computed and stored in;\nthe query type; results are TC = promote_type(TW, typeof(x)).\n\nThe key fact making the mixed-precision path exact: lifting a narrower float to a wider type is exact (Float64 → Double64/BigFloat preserves the value bit-for-bit), and lifting commutes with windowing. So a cache with Float64 nodes and weight_type = Double64 has weights identical to those computed from wide-typed nodes from the start, and window searches compare mixed types exactly. Values narrower than TC are lifted exactly per window; values wider than TC are demoted — choose weight_type at least as wide as your data.\n\nWhat extended precision buys is arithmetic headroom, not data repair: with Float64 input data the error floor is the data's sim 10^-16 rounding (times the small centered Lebesgue constant); with BigFloat data and weights the measured floor at degree 28 is sim 10^-50.","title":"Precision semantics"},{"category":"section","location":"theory/#Order-and-type-envelope","page":"Theory","text":"The unnormalized weights need exponent range: w sim h^-(s-1) up to factorial factors. Measured on step-1101 grids: Float64 is comfortable through degree 120 (maxw approx 5 times 10^76), and degree 28 works down to step sim 10^-12; Float32 at degree 28 is marginal; BigFloat/Double64 are effectively unconstrained. The constructor throws if weights overflow, rather than propagating Inf.","title":"Order and type envelope"},{"category":"section","location":"theory/#Complexity","page":"Theory","text":"operation cost\ncache build O(n cdot s^2) — independent of the number of value columns\nscalar query O(s) after O(log n) window search\nm-column query window + coefficients once, then one s times m mat-vec\n\nThe multi-column sharing is the structural advantage over one-interpolant-per- column designs: at m = 338 columns it was measured at ~340× less per-point work than per-column spline objects at extended precision.","title":"Complexity"},{"category":"section","location":"examples/#Examples","page":"Examples","text":"","title":"Examples"},{"category":"section","location":"examples/#Basic-interpolation","page":"Examples","text":"using LocalBarycentric\n\nh = 1 / 101\nnodes  = collect(3.0 .+ h .* (0:400))\nf(x)   = exp(-x) * sin(3x)\nvalues = f.(nodes)\n\ncache = LocalBarycentricCache(nodes; order = 28)\nx = 3.505\ninterpolate_local(cache, values, x), f(x)","title":"Basic interpolation"},{"category":"section","location":"examples/#Many-columns,-one-query","page":"Examples","text":"An n × m value matrix shares the window search and barycentric coefficients across all m columns — build one cache per grid, not per column:\n\nvalues2 = hcat(values, cos.(nodes), values .^ 2)   # 401 × 3\ninterpolate_local(cache, values2, x)","title":"Many columns, one query"},{"category":"section","location":"examples/#Extended-precision:-Double64-weights-over-Float64-data","page":"Examples","text":"weight_type decouples the arithmetic from the node storage. This is the pattern for a high-precision evaluation path over an existing Float64 table:\n\nusing DoubleFloats\n\ncache_hp = LocalBarycentricCache(nodes; order = 28, weight_type = Double64)\nxd = Double64(nodes[200]) + Double64(1) / 303      # query below Float64 resolution\nr  = interpolate_local(cache_hp, values, xd)\ntypeof(r), r\n\nThe result responds to query changes far below eps(Float64) — the window search compares Float64 nodes against the Double64 query exactly, and only the stencil window of values is lifted (exactly) per call.","title":"Extended precision: Double64 weights over Float64 data"},{"category":"section","location":"examples/#Fully-BigFloat","page":"Examples","text":"With wide-typed data and weights the error floor follows the working precision (measured sim 10^-50 at degree 28, 256 bits):\n\nsetprecision(BigFloat, 256)\nbnodes  = BigFloat.(nodes)\nbvalues = f.(bnodes)\nbcache  = LocalBarycentricCache(bnodes; order = 28)\nbx = (bnodes[200] + bnodes[201]) / 2\nabs(interpolate_local(bcache, bvalues, bx) - f(bx))","title":"Fully BigFloat"},{"category":"section","location":"examples/#Edge-behavior","page":"Examples","text":"Queries near the domain boundary automatically use narrower centered windows (see Theory); nothing is required from the caller:\n\nusing LocalBarycentric: stencil_window\n[stencil_window(nodes, nodes[1] + k * h + h / 2, 29; edge_min = 13) for k in (0, 3, 7, 20)]","title":"Edge behavior"},{"category":"section","location":"examples/#Exact-node-hits-and-the-overflow-guard","page":"Examples","text":"interpolate_local(cache, values, nodes[57]) === values[57]\n\nusing LocalBarycentric\ntiny = Float32.(1.0 .+ (0:40) .* 1.0f-6)   # degree 28 weights overflow Float32\ntry\n    LocalBarycentricCache(tiny; order = 28)\ncatch e\n    e\nend","title":"Exact node hits and the overflow guard"},{"category":"section","location":"examples/#Practical-guidance","page":"Examples","text":"Reuse the cache. Building is O(n · order²); do it once per grid.\nBatch columns into a matrix rather than looping interpolate_local per column.\nChoose order by your accuracy target and grid spacing; on fine grids the interior is data-floor-limited already at moderate order, and the default edge floor (degree 12) is usually right. Raise edge_order_min only on coarse grids where degree-12 truncation is visible.\nAvoid extrapolation — outside the node hull the boundary window's polynomial is evaluated, and its accuracy degrades rapidly.","title":"Practical guidance"},{"category":"section","location":"#LocalBarycentric.jl","page":"Home","text":"High-order local barycentric Lagrange interpolation of tabulated 1-D data: precomputed sliding-window weights, any Real number type (Float64, Double64, BigFloat, even Rational), a graded stencil taper at the domain edges, and per-query work shared across many data columns.","title":"LocalBarycentric.jl"},{"category":"section","location":"#Installation","page":"Home","text":"Until registration:\n\nusing Pkg\nPkg.add(url = \"https://github.com/Canonical111/LocalBarycentric.jl\")","title":"Installation"},{"category":"section","location":"#Quickstart","page":"Home","text":"using LocalBarycentric\n\nnodes  = 3.0 .+ (0:400) ./ 101            # sorted (here: equispaced)\nvalues = @. exp(-nodes) * sin(3nodes)     # Vector, or an (n × m) Matrix\ncache  = LocalBarycentricCache(nodes; order = 28)\n\ninterpolate_local(cache, values, 3.505)\n\nThe cache is built once per node grid; evaluation is O(order) per query regardless of grid size, plus one small matrix–vector product when values is a matrix — the window and coefficients are shared across all its columns.","title":"Quickstart"},{"category":"section","location":"#When-to-use-it","page":"Home","text":"Your data is given on a fixed (typically equispaced) grid — you cannot resample at Chebyshev points.\nYou need degree ≳ 10 accuracy (piecewise-cubic packages top out around h⁴).\nYou evaluate many times (hot loops) and/or many columns sharing one grid.\nYou need the same code path at Float64, Double64, or BigFloat, with a genuine extended-precision error floor.\n\nIf instead you can sample a function adaptively, use a Chebyshev package; if you need smoothing of noisy data, use a spline package. See Theory for how this scheme relates to the alternatives.","title":"When to use it"},{"category":"section","location":"#API","page":"Home","text":"","title":"API"},{"category":"module","location":"#LocalBarycentric","page":"Home","text":"LocalBarycentric\n\nHigh-order local barycentric Lagrange interpolation of tabulated 1-D data, with precomputed sliding-window weights, arbitrary-precision number types, a graded stencil taper at the domain edges, and work-sharing across many data columns per query point.\n\nThe scheme matches Mathematica's Interpolation[..., InterpolationOrder -> n] node-count convention: polynomial degree n uses a local window of n + 1 nodes centered on the query (Mathematica's window alignment differs by one node for even orders; see the README).\n\nDesigned for the workload where no existing Julia package fits: degrees ≳ 10 on equispaced tabulated grids, evaluated ~10⁴–10⁶ times in a hot loop, in Float64, Double64, BigFloat, or any Real subtype — with the same code path.\n\nnodes  = 3.0 .+ (0:400) ./ 101\nvalues = @. exp(-nodes) * sin(3nodes)          # or an (n × m) matrix of columns\ncache  = LocalBarycentricCache(nodes; order = 28)\ninterpolate_local(cache, values, 3.505)\n\n\n\n\n\n","title":"LocalBarycentric"},{"category":"type","location":"#LocalBarycentric.LocalBarycentricCache","page":"Home","text":"LocalBarycentricCache(nodes; order = 10, weight_type = eltype(nodes),\n                      edge_order_min = 12)\n\nPrecompute barycentric weights for every sliding window of order + 1 nodes over the sorted vector nodes (Mathematica InterpolationOrder convention: order is the polynomial degree). One weight row is stored per window start, so hot-loop evaluation never recomputes weights except in the tapered edge zone.\n\nweight_type sets the arithmetic type of the stored weights independently of the node storage type: e.g. Float64 nodes with weight_type = Double64 gives an extended-precision evaluation path whose weights are computed from exactly-lifted nodes (lifting Float64 → Double64/BigFloat is exact, so this equals computing in the wide type from the start).\n\nedge_order_min floors the tapered edge windows at edge_order_min + 1 nodes (default degree 12 — at that size the edge Lebesgue constant is ~10², small enough that data rounding stays near the working-precision floor while truncation remains negligible on fine grids).\n\n\n\n\n\n","title":"LocalBarycentric.LocalBarycentricCache"},{"category":"function","location":"#LocalBarycentric.interpolate_local","page":"Home","text":"interpolate_local(cache, values, x)\n\nEvaluate the local barycentric interpolant of values at x.\n\nvalues is either a length-n vector (one data column; returns a scalar) or an n × m matrix (m columns sharing the same nodes; returns a length-m vector). The window location, weights, and barycentric coefficients are computed once per query and shared across all m columns — for many-column tables this is the dominant cost advantage over per-column interpolant objects.\n\nThe result type is promote_type(weight_type, typeof(x)); values of a narrower type are lifted exactly per window, and values of a wider type are demoted to it — choose weight_type (or the query type) at least as wide as your data to avoid silent precision loss. Queries that hit a node exactly return the tabulated row unchanged (no interpolation error, no division). Queries outside the node hull evaluate the boundary window's polynomial (extrapolation — accuracy degrades rapidly; avoid by construction).\n\n\n\n\n\n","title":"LocalBarycentric.interpolate_local"},{"category":"function","location":"#LocalBarycentric.barycentric_weights","page":"Home","text":"barycentric_weights(xs) -> Vector\n\nUnnormalized barycentric weights wᵢ = 1 / ∏_{j≠i} (xᵢ - xⱼ) for the node vector xs (distinct values). O(n²); element type follows eltype(xs). Pure and deterministic: repeated calls on the same input are bit-identical.\n\n\n\n\n\n","title":"LocalBarycentric.barycentric_weights"},{"category":"function","location":"#LocalBarycentric.stencil_window","page":"Home","text":"stencil_window(nodes, x, stencil; edge_min = stencil) -> (start, width)\n\nSelect the local window for a query x over sorted nodes. Interior queries get the full centered stencil. Near a domain edge a full-width window would clamp and evaluate x at the window edge, where the degree-(stencil-1) equispaced Lebesgue function is ~2^stencil and amplifies data rounding (~10⁶ at stencil 29). The width is instead tapered to clamp(2k, min(edge_min, stencil), stencil), k being the number of nodes on the thinner side of x, keeping the query near the window center.\n\nnodes and x may have different Real types; comparisons follow Julia's exact mixed-type promotion (e.g. Float64 nodes vs Double64/BigFloat query compare exactly).\n\n\n\n\n\n","title":"LocalBarycentric.stencil_window"}]
}
